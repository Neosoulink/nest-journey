import { ExecutionContextFilter } from './execution-context.filter';

describe('ExecutionContextFilter', () => {
  it('should be defined', () => {
    expect(new ExecutionContextFilter()).toBeDefined();
  });
});
